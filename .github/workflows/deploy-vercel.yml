name: Deploy Frontend (Vercel)

# Deployment policy:
# - Trigger production deploys from this workflow only (workflow_dispatch).
# - Keep Vercel Git auto-deploy disabled (or use an ignored build step) to avoid duplicate deploy paths.
# - Do not set a static NEXT_PUBLIC_BUILD_NUMBER in Vercel project env; this workflow injects it from GITHUB_RUN_NUMBER.

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref (branch, tag, or SHA) to deploy"
        required: false
        default: "main"

concurrency:
  group: deploy-vercel-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID_SECRET: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID_SECRET: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Validate Vercel secrets
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "Missing required secret: VERCEL_TOKEN"
            exit 1
          fi
          # VERCEL_ORG_ID and VERCEL_PROJECT_ID are loaded from .vercel/project.json when available,
          # otherwise from VERCEL_ORG_ID / VERCEL_PROJECT_ID GitHub secrets.

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/package-lock.json

      - name: Load Vercel project identifiers
        run: |
          if [ -f .vercel/project.json ]; then
            VERCEL_ORG_ID=$(node -p "require('./.vercel/project.json').orgId")
            VERCEL_PROJECT_ID=$(node -p "require('./.vercel/project.json').projectId")
          else
            VERCEL_ORG_ID="$VERCEL_ORG_ID_SECRET"
            VERCEL_PROJECT_ID="$VERCEL_PROJECT_ID_SECRET"
          fi

          if [ -z "$VERCEL_ORG_ID" ] || [ -z "$VERCEL_PROJECT_ID" ]; then
            echo "Missing Vercel project identifiers. Provide .vercel/project.json or set VERCEL_ORG_ID and VERCEL_PROJECT_ID secrets."
            exit 1
          fi

          echo "VERCEL_ORG_ID=$VERCEL_ORG_ID" >> "$GITHUB_ENV"
          echo "VERCEL_PROJECT_ID=$VERCEL_PROJECT_ID" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: npm install --prefix web --no-audit --no-fund

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Validate run number
        run: |
          if ! [[ "${GITHUB_RUN_NUMBER}" =~ ^[0-9]+$ ]]; then
            echo "GITHUB_RUN_NUMBER is missing or invalid"
            exit 1
          fi

      - name: Pull Vercel environment
        run: vercel pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Build frontend
        run: |
          APP_VERSION=$(node -p "require('./web/package.json').version")
          echo "Building with NEXT_PUBLIC_BUILD_NUMBER=${GITHUB_RUN_NUMBER}"
          NEXT_PUBLIC_BUILD_NUMBER="${GITHUB_RUN_NUMBER}" \
          NEXT_PUBLIC_GIT_SHA="${GITHUB_SHA}" \
          NEXT_PUBLIC_APP_VERSION="${APP_VERSION}" \
          vercel build --prod --token="$VERCEL_TOKEN"

      - name: Deploy frontend
        run: |
          set +e
          DEPLOY_OUTPUT=$(vercel deploy --prebuilt --prod --archive=tgz --token="$VERCEL_TOKEN" 2>&1)
          DEPLOY_EXIT=$?
          echo "$DEPLOY_OUTPUT"

          if [ "$DEPLOY_EXIT" -eq 0 ]; then
            exit 0
          fi

          if echo "$DEPLOY_OUTPUT" | grep -q "api-upload-free"; then
            echo "::warning::Vercel free-tier upload limit reached. Deployment skipped; retry after cooldown window."
            exit 0
          fi

          exit "$DEPLOY_EXIT"
